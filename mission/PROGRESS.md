# PROGRESS — checkpoints, game state, defects, Studio state

## Current game state (as inherited at mission start)
- **World:** flat 320×320 single grass Part, no terrain/elevation/landmarks/audio; central hub (spawn, Melee/Defense training stations, Forge, Shop) + 4 Iron Ore nodes (south) + living-world enemy **territories** (goblin_camp east, goblin_ruins SW, wolf_den NW, chieftain_arena N).
- **Skills built:** Melee, Defense, Mining, Smithing (cap 60; curve `25*level` cumulative). Ranged/Magic/Woodcutting/Fletching/Arcana = specified-but-unbuilt.
- **Combat:** hold-attack, server-authoritative, weapon-driven; conditional aggression + provocation; living-world enemies (Goblin, Wolf, Goblin Chieftain boss) roam/aggro/chase/leash/return.
- **Items/equipment:** Weapon + Tool slots only (no armor). Starter/Upgraded Sword, Basic/Upgraded Pickaxe, Iron Ore/Bar, Chieftain Fang (rare).
- **Production:** Forge (Iron Ore→Iron Bar→Upgraded Sword). Shop buy/sell (proximity-gated). Training stations (2 tiers). Mastery trees (per-skill, points at L5/10/15/20/30/40/50/60). Ascension: Adventurer→Adept only. Offline training (Melee/Defense focus). Onboarding (guided objective chain through the whole loop).
- **Client:** mobile-polished HUD (contextual action bar, panels, toasts, target/boss bars, map-less).

## Test/playtest status
- **Repo:** 21 suites green at mission start (Phase 0 baseline verified).
- **Studio playtest:** existing loop verified end-to-end in prior sessions (spawn→train→earn→mine→smelt→gear→chieftain→Ascend→offline). Living-world enemies playtested (territories, roam/aggro/chase/leash/return, respawn-in-region, conditional aggression).

## Known defects / limitations (inherited)
- Sword-step onboarding hint understates gates (Smith Lv5 / 500g) — nonblocking pacing (from earlier acceptance).
- Ascension "Adept" needs Melee Lv8 vs ~Lv3 from guided steps — pacing spike.
- Enemy movement is a gliding mesh + bob/facing (no leg animation) — placeholder pending rigs.
- Wolf is a box placeholder — needs art.
- No audio anywhere. No armor. Single resource/production chain. One region. Flat featureless world.

## Studio scene state (Studio-owned; NOT in Git — the place MUST BE SAVED to persist)
- Place id 136843447225408; studio id 1e765b7b-6dd9-439b-bfcc-66b1688e4943; dev DataStore `AdventurersRise_PlayerData_Development` (separate from live).
- **Region I "The Marchlands" is built** (via `studio/RegionOneBuilder.luau`): terrain biomes on a flat walkable surface (~940×940, surface y≈2), scenic edge hills + NW river, `Workspace.AR_World.Scenery` folder (~144 tagged `AR_Scenery` placeholder props: trees/rocks/cottages/mine-entrance/ruined-tower/warren-totems), lighting mood set. Old flat `Ground` part removed.
- `Workspace.AR_World.Territories` markers relocated to zones (y=2): goblin_camp (160,25) pop5, goblin_ruins (255,-60) pop4, wolf_den (0,255) pop4, chieftain_arena (330,10) pop1. Iron Ore nodes → Ironrock Hollow (0,-270). AR_Spawn raised to y=2.
- **Place code synced to `feature/open-world-rpg`**: new ModuleScripts `ReplicatedStorage.Shared.RegionIds` + `RegionDefinitions`; updated `Client.Controller` + `Client.Interface`.
- **Phase 3 code synced to `feature/open-world-rpg`** — armor slice @ 4f3d7c7 + steel slice @ 8384d58. Updated instances: `ReplicatedStorage.Shared.{EquipmentSlots,ItemDefinitions,RecipeIds,GatheringNodeIds}`, `ServerScriptService.Server.{Crafting.Definitions,Composition.Service,Gathering.Definitions}`, `StarterPlayer.StarterPlayerScripts.Client.{Interface,Controller}`. World: added `Workspace.AR_World.Coal` folder with **4 coal nodes** (parts tagged `AR_GatheringNode`, attr `GatheringNodeId=node_coal`) deep in Ironrock Hollow (~z=-320). These `script.Source` + world edits live in the Edit datamodel — **the place MUST BE SAVED** to persist them.
- To re-run/rebuild the world: paste `studio/RegionOneBuilder.luau` body into the MCP execute_luau (Edit). To re-sync code: raw-fetch from the branch, set script.Source (HttpEnabled on). Sync recipe: map each `src/...` path to its instance, `HttpService:GetAsync(base .. path .. "?t="..os.time())` (cache-bust), set `.Source`; base = `https://raw.githubusercontent.com/Degrading1919/adventurers-rise/feature/open-world-rpg/`.

## BLOCKER (resolved)
- Studio place was closed; reopened and connected. Region I world built + validated in Play.

## Checkpoint log
- **[MISSION GATE — draft PR #34 opened; DoD = conditional PASS.]** Three independent full/partial DoD evaluations drove fix cycles; the final full-game re-grade lifted the verdict from NOT-YET to **conditional PASS**: **10/12 criteria ≥4** (crit 10 audio 2→4 after wiring; crits 5 & 9 defect-caveats cleared), nothing below 3, nothing broken, all newer code confirmed correctness/authority-clean. "No fixable systems gap remains." Draft PR: https://github.com/Degrading1919/adventurers-rise/pull/34 (feature/open-world-rpg → main, **do NOT merge**). Remaining gates are human/tooling, not defects: **(1) mobile device/emulator pass** (crit 12=3 — the Studio device emulator can't be driven from this automation; a human must view the phone viewport across HUD/panels/map and confirm), **(2) accept the flat-terrain D7 tradeoff** (crit 2=3). Non-blocking polish backlog recorded in PLAN. **⚠️ SAVE THE PLACE** to persist the 4 new enemy meshes + synced scripts.

- **[Phase 5 — comprehensive DoD evaluator + fixes (audio, enemy meshes, weapon placeholder, endgame goal)] DONE + STUDIO-VERIFIED.** A full-game independent evaluator graded all 12 DoD criteria: 9/12 ≥4, verdict **NOT YET** — must-fix #1 audio (crit 10=2), then world fidelity (crit 2=3) + mobile (crit 12=3); all newer code validated clean (Ascension perks, Cleave, Prospecting carry, Discovery txn, Resilience regen). Fixes applied + verified: **(1) AUDIO (crit 10, #1 lever):** committed+wired `SoundController` — a data-driven singleton over engine-shipped `rbxasset://sounds/*` (verified to load; no marketplace dep), fired on discovery/banner/boss-reward/level-up/objective (Interface) + hit/craft/equip/ascend (Controller). **Live: sounds load + play** (LevelUp/Hit/Discovery playing=true; the region banner auto-fires on spawn). **(2) WORLD FIDELITY (crit 2/7):** generated + integrated **real meshes for the 4 box-placeholder enemies** (wolf, hobgoblin, bandit, bandit_warlord) via the Studio mesh generator, wrapped as `ServerStorage.AR_Assets.Enemies.*` templates (goblin/chieftain already had meshes). **Live: the wolf renders as a proper grounded grey wolf** ("Wolf Lv 2" bar) — `seatModelOnMarker` grounds each by real AABB so pivots don't matter. **(3) crit 5:** procedural held-weapon/tool placeholder in EquipmentVisuals (metal blade / wooden haft) — **live: the wooden pickaxe shows in the player's hand**. **(4) crit 9/11:** the objective banner no longer blanks at max rank (forward "new frontiers" goal). Also confirmed the evaluator's fractional-health cosmetic is already handled (client rounds health at both display sites). **23 suites green; compile clean.** REMAINING (documented): terrain-elevation (crit 2 play-surface — enemy-flat-ground tradeoff; enemies now AABB-grounded so would follow elevation), mobile device pass (Studio emulator not MCP-drivable), ambient loops. **⚠️ The 4 new enemy meshes are Studio-owned (ServerStorage.AR_Assets.Enemies) — MUST SAVE THE PLACE to persist**, along with the synced SoundController/Interface/Controller/EquipmentVisuals scripts.

- **[Phase 5a/5b — armor body visual + map polish] DONE + STUDIO-VERIFIED (screenshot).** 5a: `EquipmentVisuals` now renders equipped armour as tinted **metal shells** welded over Head/UpperTorso/LowerTorso (R15, R6 fallback), tier-coloured (iron grey, steel blue-grey) — the evaluator's biggest remaining Criterion-5 gap (armour was cosmetically inert) is closed; same clean reconciliation + replacement seam as the weapon/tool templates. **Live-verified via screenshot:** drove the real EquipmentVisuals with a steel set → 3 shells (head/body/legs) visible as blue-grey plating on the character. 5b: map zone/boss labels anchor toward the interior near the panel edges (long far-east names no longer center-clip). 23 suites green; compile clean. **CONSTRAINTS (tooling, documented like the _G seeding limit):** **5c mobile** — Studio's device emulator isn't MCP-drivable, so visual phone validation is a manual pass; all new UI is mobile-adaptive by construction (`capSize` + scale-based UDim2, same as the mobile-first base HUD). **5d audio** — still absent (criterion 10); needs chosen sound assets that can't be verified by ear here — best done as a dedicated audio pass with real assets. Synced EquipmentVisuals + Interface — **SAVE THE PLACE** (note: a CDN-stale Interface sync may have missed the 5b label tweak; re-sync from the branch if labels still clip).

- **[Phase 4e — activate dead Mastery branches: ALL 4 DONE + STUDIO-VERIFIED.]** Evaluator rec #3 complete. Beyond Merchant Smith (sale bonus) + Resilience (regen), the two "harder" branches are now live too: **Melee Cleave → multi-target splash** — a landed melee hit splashes `MeleeCleaveDamageBonus × damage` onto enemies within 12 studs of the primary; new `World.CleaveTargetsProvider` (nearby-enemies query) + `Enemies.CreateCleaveResolver` (ApplyDamage closures) + Combat applies it after the primary hit, fully decoupled/injected; cleave is crowd-control (secondary hits bypass the reward/death-queue path — a cleaved kill just dies+respawns, no Gold, no leak), never affects the primary result/cooldown/XP. `AttackResult.CleaveHits` added. **Mining Prospecting → raw-gem byproduct** — done within Gathering's deterministic (no-RNG) model: `RareResourceBonus` yields raw gems as a fraction of the ore's exact value, carried across settlements like ore (chunk-size independent), best-effort (a gem overflow never rolls back ore); new `item_raw_gem` (Stackable) sold-only in the shop for 18 Gold — a distinct payoff (Gold) vs Extraction (ore for crafting). Tests: Composition end-to-end Cleave proof (CleaveHits 0→1, secondary damaged), Gathering gem proof (1 gem/2-ore @ 50% + sub-unit carry), Inventory whitelist → 18. **23 suites green; compile clean. Live:** clean boot with all wiring (new world adapter + Combat cleave param + gem item/shop); in-engine check — **0 Mastery nodes still "reserved"** (all 4 branches active), gem item + 18-Gold sell entry present, living-world loop (deaths + attacks + regen + cleave provider) healthy at 24 enemies. Synced 13 scripts — **SAVE THE PLACE**.

- **[Phase 4e — activate dead Mastery branches (2 of 4)] DONE + STUDIO boot-verified.** (superseded by the all-4 checkpoint above) Evaluator rec #3: 4 Mastery branches emitted effect keys no system consumed. Activated the **two cleanest** with real, tested effects: **(1) Smithing "Merchant Smith" → shop sale-value bonus** — Economy scales SELL payouts (stack + discrete) by the player's `CraftedSaleValueBonus`, read from the same transactional data the sell mutates (wired via a forward-declared `mastery` handle since Economy is built before Mastery). Test: 25% bonus raises Ore 4→5, Sword 50→63. **(2) Defense "Resilience" → out-of-combat regeneration** — `RecoveryBonus` regenerates a below-max living player's health over time (scaled by their own max), ticked by World's shared poll on the same cadence as enemy attacks; PlayerHealth stays pure (World drives it), `World.Start` gains an optional playerHealth arg (2-arg callers unaffected), no RecoveryBonus = no regen so baseline is unchanged. Test: heal 60→65 in 1s @ 10% recovery, never overshoots, no-bonus/full/dead never heal. Both node descriptions updated from "reserved for future". **23 suites green; compile clean. Live:** clean boot; the living-world poll loop (deaths + attacks + **regen**) healthy — 24 enemies present after several ticks, no errors. (Live regen not shown: the dev player is Defense L4, one short of the L5 Resilience node gate — same seeding limit; covered by the automated PlayerHealth test.) **DEFERRED (need larger new systems, honest to stage as their own slices):** **Mining "Prospecting" → rare resource** (Gathering is deliberately RNG-free/deterministic; needs either a determinism-safe design or a new gem item+shop+fractional-carry) and **Melee "Cleave" → multi-target** (needs a real multi-target combat hook in CombatRewards/World/Enemies — the evaluator's "meatiest"; combat-loop regression risk warrants a dedicated slice). Synced Economy/Composition/Mastery/PlayerHealth/World/Bootstrap — **SAVE THE PLACE**.

- **[Phase 4d — Ascension rank perks: a visible payoff] DONE + STUDIO-VERIFIED (screenshot).** Evaluator rec #2: Ascending relabeled but didn't reward. Now each rank grants a permanent, felt **Perk {MaxHealthBonus, IncomingDamageReduction}** (Adept +8% max HP; Vanguard +20% max HP + 5% damage reduction), validated in `Ascension/Definitions`. **Composition folds the CURRENT rank's perk into the SAME defense-modifier channels armor/Mastery use** (ascension forward-declared so the provider can read it), so Ascending immediately raises max health + cuts damage with NO change to PlayerHealth/EnemyCombat (least-regression). Read model surfaces `Ascension.RankPerk` (held now) + `NextRank.Perk` (what Ascending grants); the Ascend panel now shows the **current→next header** (also fixes a silent CRLF miss from 4a), an **"Ascending grants: +N% Max HP, …"** line, and the held-rank perk at max rank. Tests: perk exposure (current+next) + a **Composition end-to-end proof that Ascending to Adept raises MaxHealth 100→108** through the real stack. **23 suites green; compile clean. Live:** clean boot; read model returns RankPerk {0,0} / NextRank.Perk {0.08,0}; **screenshot of the Ascend panel shows "Adventurer ▶ Adept" + green "Ascending grants: +8% Max HP" + the requirement checklist.** Synced Ascension Definitions+Service, Composition Service+ReadModel, Client.Interface — **SAVE THE PLACE**. Remaining evaluator rec: **#3 activate the 4 dead Mastery branches** (larger; may be Phase 5).

- **[Phase 4c — evaluator fix cycle: route the player + coherence fixes] DONE + STUDIO-VERIFIED (screenshot).** Independent Phase-4 evaluator graded crit 4=3 / 6=3 / 9=4 and said **DON'T build "more Mastery nodes"** (the trees are broad-but-hollow: 1/3 of branches inert, ±2-3% imperceptible effects, no endgame choice) — instead **route the player** (its #1 lever) + fix two coherence issues. Applied: **(1) Routing thread (client, biggest lever):** post-onboarding the objective banner becomes a live "Next rank: <rank>" goal naming the single most salient unmet requirement — **boss first with a PLACE** ("Defeat the Bandit Warlord in the Ashen Ruins"), then skill gates; tapping it opens the Ascend panel. The **map** now plots both boss arenas (red diamonds, from their territory markers) and **rings the zone holding the boss the next rank needs** (green ring + "▶" label). So "become Vanguard" now points at a spot on the map. **(2) Ascension chain validator:** `Validate` enforces a single linear rank chain (one start, ≤1 successor/rank, acyclic) so the service's next-rank pick is always well-defined (closes the latent nondeterminism the evaluator flagged) — new negative test. **(3) Discovery rewards rescaled** to rise monotonically with danger (RecommendedLevel): Ironrock 25 < Woods 40 < Meadows 55 < Ruins 90 < Warren 150 (was incoherent + trivial). **23 suites green; compile clean. Live:** clean boot (validator + discovery Validate passed); opened the map in Play — Bandit Warlord + Goblin Chieftain diamonds shown, **green objective ring + "▶ Goblin Warren"** on the Adventurer's next-rank target (defeat the Chieftain). Residual (cosmetic, Phase 5): far-east labels crowd/clip at the panel edge. Deferred evaluator recs: **#2 Vanguard visible payoff** (rank perk on Ascend) and **#3 activate the 4 dead Mastery branches** (not add a 4th tier). Synced Ascension/Discovery Definitions + Client.Interface — **SAVE THE PLACE**.

- **[Phase 4b-2 — region map panel] DONE + STUDIO-VERIFIED (screenshot).** A schematic top-down **Map** panel (Menu → Map), client-only over data the server already owns: plots each Region I zone by its world centre (`RegionDefinitions`), coloured by difficulty tier; **discovered** zones show their name, **undiscovered** show "???" (driven by read-model `Discovery.DiscoveredZoneIds`); a **live player marker** updates each proximity tick (no-op while closed). Added `iface.UpdateMapMarker` + `menuEntry("Map")`; `hideAllPanels` includes it. **Live-verified via screenshot:** opened the panel in Play (drove the real Menu→Map buttons by AbsolutePosition) — Havenbrook + Green Meadows + Goblin Warren shown by name/tier-colour, the 3 unvisited zones as "???", player marker on Havenbrook. Fixed a label-overlap on the adjacent eastern zones (stagger labels by index). 23 suites green; compile clean. Residual (minor): the far-east Warren label clips slightly at the panel edge — cosmetic, a Phase-5 polish item. Synced Client.Interface + Controller — **SAVE THE PLACE**.

- **[Phase 4b-1 — first-visit zone discovery + rewards] DONE + STUDIO-VERIFIED.** Exploring a wilderness zone for the first time grants a one-time Gold reward (Meadow/Forest 30, Hills 40, Ruins 60, Warren 80) + records a permanent discovery flag — fully server-authoritative. New `Discovery` service + data-driven `Discovery/Definitions` (per-zone reward, town excluded) + 5 discovery flags in ProgressionFlagIds; grant is **atomic** (flag + Gold credit in one PlayerData transaction) and **idempotent** on re-entry. **Anti-spoof:** the zone is resolved from the player's REAL position via a new `World.PlayerZoneResolver` adapter (RegionDefinitions.ZoneAt), never from client input; the new `RegionDiscoverCurrentZone` request carries no trusted zone id. Read model surfaces `Discovery.DiscoveredZoneIds` (for the map). Client auto-asks to discover on genuine zone entry + toasts the reward (`iface.ShowDiscovery`). RegionIds iteration cast so it type-checks. Tests: a **standalone Discovery suite (5 checks)** + Composition coverage (request path, idempotency, anti-spoof, read-model surface); runners updated (composition + world embed the region modules; new run_discovery.py). **23 suites green; compile clean. Live in the place:** clean boot; teleported the character into the Meadow → the client's zone-entry hook auto-fired discovery → **+30 Gold**; into the Warren → **+80 Gold** (gold 30→110); discovered set = {meadow, warren}; re-request correctly returned NewlyDiscovered=false (idempotent). Synced 12 scripts incl. 2 new Discovery ModuleScripts — **SAVE THE PLACE**. NOTE: this granted the dev player meadow/warren flags + gold in the dev store.

- **[Phase 4a — Ascension II: Vanguard + generalized next-rank progress] DONE + STUDIO-VERIFIED.** Added the mid-game **Vanguard** rank (AscensionIds + Ascension/Definitions) chaining from Adept, gated on content already built: Melee 15, Defense 12, Mining 8 + Smithing 10 (the steel tier), and the **Bandit Warlord** boss milestone (2nd boss) — a real end-game goal that requires working the whole loop. Generalized away the Adept hardcoding: Ascension `GetState` now carries `DisplayName`; new `EvaluateNextRank(FromData)` returns progress toward the player's NEXT rank (or `(nil,nil)` at the top) via generic PreviousRankId chaining. Read model now surfaces `Ascension.RankDisplayName` + `Ascension.NextRank {RankId, DisplayName, Progress}` (was hardcoded `AdeptProgress`). Client Ascend panel is next-rank-driven: names the target on the button ("Ascend to Vanguard"), shows current→next, generic boss-milestone labels (chieftain/warlord), a "max rank" state; the request sends the resolved next-rank id (client no longer imports AscensionIds). Also synced the deferred **0.9-clamp de-dup** (EnemyCombat exports `MaxIncomingDamageReduction`). Tests: Vanguard chain/reqs, GetState.DisplayName, EvaluateNextRank chaining + top-of-ladder, a full Adept→Vanguard ascend, updated read-model assertion. **22 suites green; compile clean. Live:** clean boot (Vanguard passed Validate under real Roblox); the client RemoteFunction returns the new Ascension shape (Adventurer→Adept, Defense req 5, chieftain milestone); in-engine require confirms Vanguard chains from Adept with the right gates + Validate=true. Synced 8 scripts into the place — **SAVE THE PLACE**.

- **[Phase 3 — CLOSED. Evaluator re-grade: ADVANCE.]** After the legibility fix cycle, the independent evaluator re-graded: **criterion 5 = 4** (was 3 — both weapon+armour tracks now legible; held at 4 not 5 only for the missing armour body visual + modest magnitude), **criterion 8 = 4**, **criterion 9 = 4** (was 3 — Forge shows gated steel recipes with their stat upgrade; held at 4 for the still-absent map). It verified the fixes in code (defenseSummary single-sources the same clamp combat uses; read-model Defense block; Gear/Forge readouts) and confirmed the engineering it validated earlier is untouched. **VERDICT: advance to Phase 4.** Carry to Phase 5 (non-blocking): armour body visual (biggest remaining Criterion-5 gap; EquipmentVisuals seam ready), a visceral harder-content signal; **done now**: de-dup the 0.9 clamp (EnemyCombat exports `MaxIncomingDamageReduction`; Composition clamps to it). 22 suites green.

- **[Phase 3 — evaluator fix cycle: armor legibility] DONE + STUDIO-VERIFIED.** Independent Phase-3 evaluator (did not implement) graded: criterion 8 (gather→produce) = 4 PASS; criteria 5 (weapons+armor visibly ease) & 9 (visible progress) = 3, BLOCKED on legibility — armor changed no on-screen number and no body visual, and a code comment overclaimed a hard zone "gate". Engineering validated clean (correct table.clone, single-read/no-double-count, 0.9 clamp, live unequip, server-only). **Fixes:** (1) read model now returns a `Defense` block — `DamageReduction` (the exact mitigation EnemyCombat applies = Defense Mastery + armor, clamped) + `ArmorDamageReduction` (equipped-gear-only share) — from a single server source (`Composition.defenseSummary`, reading the same `defenseModifiers` combat uses, same 0.9 ceiling), so the number can't drift from combat. (2) Client: Gear panel shows "Damage reduction: N% (armour M%)" + each piece's own contribution (armour %/weapon dmg); Forge rows show the output's stat so the iron→steel ladder reads as an upgrade on the recipe list. (3) Reworded the overclaiming steel-armor comment to honest soft-easing. **Deferred (evaluator marked optional):** armor BODY visual → Phase 5 (needs AR_Assets.Equipment templates; EquipmentVisuals seam already present, Weapon/Tool only). Test: armor integration test now also asserts the read model reports 0.16 for a full iron set. **22 suites green; compile clean. Live:** the real client RemoteFunction now returns the `Defense` block (0/0 unarmored dev player = correct); nonzero path covered by the automated test. Synced 4 scripts into the place (Composition Service+ReadModel, ItemDefinitions, Client Interface) — **SAVE THE PLACE**. Re-grade of criteria 5/9 requested from the evaluator.

- **[Phase 3 — steel tier + 2nd resource (Coal)] DONE + STUDIO-VERIFIED.** Gear is now a real ladder (starter → iron → steel) behind a second resource. Data on existing systems: **Coal** node (`node_coal`, Mining Lv3, `item_coal`) as a 2nd `Gathering` definition + `GatheringNodeIds`; **Steel Bar** smelted from Iron Bar **+** Coal (first two-input recipe); **Steel Sword** (22 dmg > iron's 15) + **steel armour set** (helm 0.06 / body 0.10 / legs 0.08 = **0.24** > iron's 0.16), all Smithing-gated above iron (Steel Bar Lv8 → gear Lv9-12). Client: mining now gathers **whichever node the player stands at** (`currentNodeId` from the tagged node's `GatheringNodeId`) instead of a hardcoded Iron Ore; new item/node labels. World builder: seeds **4 coal rocks deep in Ironrock Hollow** by the hobgoblins (danger-gated resource); live place seeded the same (incremental, no full rebuild). Tests: Inventory → 17 items (steel beats iron per-piece), Crafting asserts the two-input Steel Bar + steel chain, Gathering asserts coal gated above iron. **Live in the place:** clean boot; the real client RemoteFunction returns all **5 steel recipes** with correct gates/inputs; **`GatheringBegin(node_coal)` → `InsufficientMiningLevel`** (server found + level-gated the coal node — full resolve path) and iron → `NodeOutOfRange` (both nodes resolve); in-engine `require` confirms steel set 0.24 > iron 0.16, steel sword 22 > 15, and Crafting+Gathering `Validate` = true. **22 suites green; src+builder compile clean.** Same caveat as the iron slice (no hands-on high-level craft; dev player is Mining/Smithing Lv1). Synced 7 scripts + 4 coal parts into the place — **SAVE THE PLACE**.

- **[Phase 3 — iron armor slice] DONE + STUDIO-VERIFIED (with one seeding caveat).** Live verification in the place (feature/open-world-rpg synced): (a) server + client **boot clean, no errors** — the new items/recipes passed `Validate` under real Roblox at bootstrap; (b) driving the **real client RemoteFunction** (`ReplicatedStorage.Remotes.Request` → `StateGet`) as the live player returns **all 3 armor recipes** in the authoritative read model (Iron Helm Smith Lv4 / 2 bars, Iron Greaves Lv6 / 3 bars, Iron Chestplate Lv7 / 4 bars) — the Forge panel will render them; (c) **in-engine** require of the synced modules confirms the armor items load with correct slots + reductions (0.04/0.07/0.05 = 0.16), Head/Body/Legs slots present, `Crafting.Validate` = true, and the reduction math yields a **16% cut** (10 dmg → 8.40) under the EnemyCombat clamp. CAVEAT: a hands-on craft→equip→get-hit by a leveled player was NOT run live because the dev-store player is fresh (Smithing Lv1, no bars) and the MCP execute_luau `_G` is sandboxed away from the running server, so PlayerData could not be seeded to fast-forward; the equip→reduce path is instead proven by the **automated Composition integration test** which runs the identical real composed EnemyCombat + defenseModifiers + Equipment + ItemDefinitions stack and asserts the exact 16% reduction. Code synced into the place (6 scripts, see scene state) — **SAVE THE PLACE** to persist. Original code-complete detail follows.

- **[Phase 3 — iron armor slice] CODE-COMPLETE.** Added Head/Body/Legs equipment slots (`Shared/EquipmentSlots`, generic Equipment/Service needs no change). Added an `Armor{DamageReduction}` item stat + factory (`Shared/ItemDefinitions`) and the iron set: `armor_iron_helm` 0.04, `armor_iron_body` 0.07, `armor_iron_legs` 0.05 (full set = 0.16). Combat wiring: `Composition/Service.defenseModifiers` now folds equipped-armor reduction into the SAME `IncomingDamageReduction` channel EnemyCombat already reads+clamps — EnemyCombat/PlayerHealth are UNCHANGED (least-regression); a cloned modifier table means Mastery's cached totals are never mutated, and MaxHealthBonus passes through untouched. Smithing recipes added (`Shared/RecipeIds` + `Crafting/Definitions`): Iron Helm (Smith Lv4, 2 bars), Iron Greaves (Lv6, 3 bars), Iron Chestplate (Lv7, 4 bars). Client: `ITEM_LABELS`+`SLOT_LABELS` extended; Gear panel + Forge are data-driven so armor appears/equips/crafts automatically; `EquipmentVisuals` iterates only Weapon/Tool so armor has no body visual yet (clean seam for later rigs). Tests: Inventory whitelist → 11 items (+3 armor, asserts slots+reduction); Crafting spec asserts the 3 armor recipes; **new Composition integration test proves equipped iron set reduces a live enemy hit by exactly 16% through the real composed pipeline**. **22 suites green; whole-source luau-compile clean; git diff --check clean.** PENDING: Studio sync + live craft/equip/damage playtest + regression pass + SAVE THE PLACE.

- **[Phase 2 — enemy ecology + combat feel] DONE.** Added Hobgoblin (Lv6-12, deep Ironrock Hollow), Bandit (Lv5-11, Ashen Ruins), and **Bandit Warlord** (2nd shared boss, fixed Lv12, ruined watchtower) as data on the reusable systems (Enemies/Bosses Definitions, EnemyIds, ItemDefinitions `item_warlord_seal`, ProgressionFlagIds `BanditWarlordDefeated`). World/Service: data-driven display names + placeholder box silhouettes + generalized boss detection. Client: boss bar + reward toast + rare-loot badge generalized (verified "Bandit Warlord 520/520" bar in Play). Builder: 7 territories (adds hobgoblin_hollow, bandit_camp, warlord_tower); the two combat-empty zones are now real threats; onboarding mine kept safe. **Combat hit feedback**: white flash + neon spark on landed hits (client presentation). Live census: wolves(3-4)/goblins(3-8)/hobgoblins(8-10)/bandits(5-9)/Chieftain(10)/Warlord(12) = 24 enemies. 22 suites green; console clean. **Audio still deferred** (dedicated pass). NOTE: normal enemies drop Gold only + bosses drop rare items (on-design; enemy material-drops deferred to Phase 3's material ladder).

- **[Phase 0]** Mission control system created (`mission/` artifacts + DoD rubric). Branch `feature/open-world-rpg` cut off `feature/living-world-enemies`. Baseline: 21 suites green.
- **[Phase 1 — repo prep]** Added `Shared/RegionIds` + `Shared/RegionDefinitions` (Region I + 6 zones) with a pure `ZoneAt(x,z)` classifier + spec (6 checks). Authored `studio/RegionOneBuilder.luau`.
- **[Phase 1 — WORLD BUILT + VALIDATED]** Built Region I; added region banner + objective beacon; zoned populations; 22 suites green. Independent evaluator graded it: criteria 2 (distinct places) & 3 (gradient) at 2/5 — gradient was cosmetic, world flat, scenery non-colliding, empty "hard" zones. Verdict: fix before Phase 2.
- **[Phase 1 — FIX CYCLE (evaluator)]** (1) **Real gradient:** `Enemies.Create` takes an optional level band clamped into the archetype range; territory markers carry `LevelMin/Max` (World reads + reuses on respawn). Verified live: wolves Lv2-4, near goblins Lv3-4, deep goblins Lv6-8, Chieftain Lv10 — distance = difficulty. New Enemies test. (2) **Honest zones:** blurbs/levels no longer promise absent enemies (Ironrock=mining, Ashen Ruins=foreboding-empty). (3) **World v2:** roads radiate town→each zone; central **keep tower** (relocated to SW off the spawn) as region-wide sightline anchor; tall rock-spire landmarks; denser props; **solid (colliding) scenery**. (4) **Banner** dark backdrop + stronger strokes. (5) **Fresh Lv1-spawn playtest:** fresh player spawns in town, onboarding + objective **beacon** correctly guide to combat across the bigger world (beacon points to the Melee station / goblins). **22 suites green, no regression, console clean.** Criterion 3 now ~4 (real gradient); criterion 2 much improved (landmarks/paths/verticality/biomes) — residual: play-SURFACE is still flat (vertical relief is scenery/hills/tower, not terrain elevation) — deferred to a terrain-elevation pass. **NEXT:** Phase 2 (enemy ecology: Bandit in Ashen Ruins, Hobgoblin in Ironrock, 2nd boss Bandit Warlord, loot tables, combat hit/impact feedback); a full evaluator re-assesses world+ecology at Phase-2 end.
